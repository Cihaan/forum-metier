{% extends 'base.html.twig' %}

{% block title %}Test1 index{% endblock %}

{% block body %}
    <h1>Test1 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for test1 in test1s %}
            <tr>
                <td>{{ test1.id }}</td>
                <td>{{ test1.name }}</td>
                <td>
                    <a href="{{ path('app_test1_show', {'id': test1.id}) }}">show</a>
                    <a href="{{ path('app_test1_edit', {'id': test1.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_test1_new') }}">Create new</a>
{% endblock %}
